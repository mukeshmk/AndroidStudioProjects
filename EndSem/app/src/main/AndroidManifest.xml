<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mk.endsem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" />
        <activity android:name=".AdminPage" />
        <activity android:name=".AdminAdd" />
        <activity android:name=".AdminDelete" />
        <activity android:name=".AdminMod" />
        <activity android:name=".AdminModify" />
        <activity android:name=".DeptPage" />
        <activity android:name=".DeptView" />
        <activity android:name=".DeptLeave"></activity>
    </application>

</manifest>